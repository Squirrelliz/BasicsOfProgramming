#include <stdio.h>
#include "libs/data_structures/string/tests_string.h"

int main() {
    tests_tasks();
   //char beginString[] = "I'm2in3Californi00a1";
   //char *endStringBuffer = copy(beginString, str + strlen_(str), _stringBuffer);
   //puts(_stringBuffer);
   //char *beginStringBuffer = _stringBuffer;
   //endString = beginString;
   //while (endString - beginString <= MAX_STRING_SIZE && *beginStringBuffer != '\0') {
   //    if (isdigit(*beginStringBuffer)) {
   //        for (int i = 0; i < *beginStringBuffer; ++i){
   //            *endString = ' ';
   //            endString++;
   //        }
   //    } else {
   //        *endString = *beginStringBuffer;
   //        endString++;
   //    }

   //    beginStringBuffer++;
   //}
   // puts(beginString);
    return 0;
}
